namespace Vaseis
{
    /// <summary>
    /// Provides enumeration over the user types
    /// </summary>
    public enum UserType
    {
        Administrator = 0,
        Employee = 1,
        Evaluator = 2,
        Manager = 3
    }
}
